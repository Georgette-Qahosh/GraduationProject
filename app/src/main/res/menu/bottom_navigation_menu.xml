<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/profile_menu"
        android:enabled="true"
        android:icon="@drawable/ic_user_bottom_menu"
        android:title="Profile"/>
    <item
        android:id="@+id/home_menu"
        android:enabled="true"
        android:icon="@drawable/ic_home_bottom_menu"
        android:title="Home"/>
    <item
        android:id="@+id/chat_menu"
        android:enabled="true"
        android:icon="@drawable/ic_chat_bottom_menu"
        android:title="Chat"/>
</menu>